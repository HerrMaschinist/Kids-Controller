# KIDS_CONTROLLER – Datenbankdokumentation

## Verbindung

| Parameter | Wert |
|---|---|
| Host | `192.168.50.10` |
| Port | `5432` |
| Datenbank | `kids_controller` |
| Rolle | `kids_controller_admin` |

---

## ENUM-Typen

### `window_status_enum`
```sql
'ACTIVE', 'COMPLETED'
```
Kein weiterer Wert. Insbesondere kein `ABORTED`.

### `mode_enum`
```sql
'TRIPLET', 'PAIR', 'SINGLE', 'SKIP'
```

### `perm_code_enum`
```sql
'123', '132', '213', '231', '312', '321'
```
Codiert die Reihenfolge der drei Kinder: Stelle 1 = pos1, Stelle 2 = pos2, Stelle 3 = pos3.
Ziffern sind Kind-IDs (1=Leon, 2=Emmi, 3=Elsa).

### `pair_key_enum`
```sql
'12', '13', '23'
```
Identifiziert das anwesende Zweier-Paar nach Kind-IDs.

---

## Tabellen

### `fairness_windows`

Speichert aktive und abgeschlossene Dreier-Fairnessfenster.

| Spalte | SQL-Typ | NULL | Beschreibung |
|---|---|---|---|
| `id` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NOT NULL | Primärschlüssel |
| `window_id` | `CHAR(8)` | NOT NULL | Kurzkennung (alphanumerisch, kein UUID) |
| `window_start_date` | `DATE` | NOT NULL | Erstes Datum dieses Fensters |
| `window_status` | `window_status_enum` | NOT NULL | ACTIVE oder COMPLETED |
| `window_index` | `SMALLINT` | NOT NULL | Fortschritt: 0–11 = ACTIVE, 12 = COMPLETED |
| `window_size` | `SMALLINT` | NOT NULL | Immer 12 |
| `permutation_sequence` | `JSONB` | NOT NULL | Array der 12 Perm-Codes in zufälliger Reihenfolge |
| `last_full_order` | `perm_code_enum` | NULL | Letzte vollständige Dreier-Permutation |
| `last_full_draw_date` | `DATE` | NULL | Datum des letzten Dreier-Draws |
| `last_mode` | `mode_enum` | NULL | Letzter TRIPLET-/Fensterzustand, kein globaler Gesamtmodus |
| `seed_material_hash` | `CHAR(64)` | NOT NULL | SHA-256-Hash des Seed-Materials |
| `shuffle_algorithm` | `VARCHAR(64)` | NOT NULL | `fisher_yates` |
| `algorithm_version` | `VARCHAR(32)` | NOT NULL | z.B. `1.0.0` |
| `created_at` | `TIMESTAMPTZ` | NOT NULL | Erstellungszeitpunkt |
| `updated_at` | `TIMESTAMPTZ` | NOT NULL | Letzter Änderungszeitpunkt |

### `draws`

Jeder einzelne Auslosungs-Vorgang, unveränderlich gespeichert.

| Spalte | SQL-Typ | NULL | Beschreibung |
|---|---|---|---|
| `id` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` | NOT NULL | Primärschlüssel |
| `draw_ts` | `TIMESTAMPTZ` | NOT NULL | Zeitpunkt der Auslosung |
| `draw_date` | `DATE` | NOT NULL | Datum der Auslosung |
| `request_id` | `UUID` | NOT NULL | UUID des Aufrufers; niemals NULL; UNIQUE |
| `window_id` | `CHAR(8)` | NULL | Referenz auf fairness_windows.window_id |
| `mode` | `mode_enum` | NOT NULL | TRIPLET / PAIR / SINGLE / SKIP |
| `present_mask` | `SMALLINT` | NOT NULL | Bitmaske: 1=Leon, 2=Emmi, 4=Elsa (kein JSON) |
| `window_index` | `SMALLINT` | NULL | Nur bei TRIPLET gesetzt (0–11), sonst NULL |
| `active_window_index_snapshot` | `SMALLINT` | NULL | Fenster-Index zum Zeitpunkt des Draws |
| `perm_code` | `perm_code_enum` | NULL | Nur bei TRIPLET gesetzt, sonst NULL |
| `derived_from_last_full_order` | `BOOLEAN` | NOT NULL | PAIR aus TRIPLET abgeleitet |
| `is_effective` | `BOOLEAN` | NOT NULL | Effektiver Draw (Standard: TRUE) |
| `superseded_by_draw_id` | `BIGINT` | NULL | FK auf draws.id bei Überschreibung |
| `pair_key` | `pair_key_enum` | NULL | Nur bei PAIR gesetzt, sonst NULL |
| `pair_cycle_index` | `SMALLINT` | NULL | 0 oder 1, nur bei PAIR, sonst NULL |
| `pos1` | `SMALLINT` | NULL | Kind-ID auf Position 1 |
| `pos2` | `SMALLINT` | NULL | Kind-ID auf Position 2 |
| `pos3` | `SMALLINT` | NULL | Kind-ID auf Position 3; nur bei TRIPLET |
| `stop_morning` | `SMALLINT` | NULL | Kind-ID Haltestelle morgens |
| `stop_midday` | `SMALLINT` | NULL | Kind-ID Haltestelle mittags |
| `algorithm_version` | `VARCHAR(32)` | NOT NULL | z.B. `1.0.0` |
| `seed_material_hash` | `CHAR(64)` | NOT NULL | SHA-256 des Request-Seed-Materials |
| `replay_context_hash` | `CHAR(64)` | NOT NULL | SHA-256 des normalisierten Replay-Kontexts |
| `note` | `VARCHAR(255)` | NULL | Optionale Notiz |

### `system_config`

Schlüssel-Wert-Konfiguration. Felder exakt: `key_name`, `value`, `updated_at`.
Aktuell ist sie Seed-/Referenzdatenbestand; der Runtime-Pfad liest diese Werte nicht.

| Spalte | SQL-Typ | NULL | Beschreibung |
|---|---|---|---|
| `key_name` | `VARCHAR(64)` | NOT NULL | Konfigurationsschlüssel (PK) |
| `value` | `VARCHAR(255)` | NOT NULL | Konfigurationswert |
| `updated_at` | `TIMESTAMPTZ` | NOT NULL | Letzte Änderung; beim Upsert explizit setzen |

Verboten sind `config_key`, `config_value` und JSONB als Value-Typ.

---

## present_mask – Bitmaskenlogik

`present_mask` ist `SMALLINT`, kein JSON:

| Wert | Bedeutung |
|---|---|
| 0 | niemand |
| 1 | nur Leon |
| 2 | nur Emmi |
| 4 | nur Elsa |
| 3 | Leon + Emmi |
| 5 | Leon + Elsa |
| 6 | Emmi + Elsa |
| 7 | alle drei |

---

## Algorithmus-Regeln

**TRIPLET:** 12er-Fenster, jede der 6 Permutationen genau 2 Mal. Fisher-Yates-Shuffle.
`window_index` läuft 0–11 (ACTIVE), danach 12 (COMPLETED). Rückkehrregel: bestehendes
Fenster wird fortgesetzt, wenn wieder alle drei anwesend sind. `last_mode` ist dabei
nur der letzte Fenster-/TRIPLET-Zustand.
Die Fensterfolge ist reproduzierbar, weil die Shuffle-Sequenz aus dem gespeicherten
Fenster-Seed abgeleitet wird.
Der Draw speichert zusätzlich `replay_context_hash`, also den SHA-256-Fingerprint
des normalisierten Eingabe- und Referenzkontexts für Audit und Replay.

**PAIR:** Das Dreierfenster wird nicht verbraucht. AB/BA-Rotation per `pair_cycle_index`.
Erster PAIR-Tag einer zusammenhängenden Phase kann aus `last_full_order` abgeleitet werden.
Wenn kein aktives Fenster mehr vorhanden ist, wird der letzte effektive TRIPLET-Draw
als fachliche Referenz herangezogen. `window_index` ist NULL. `perm_code` ist NULL.
`pos3` ist NULL. Die Runtime-Wahrheit liegt in `core/draw_service.py::_handle_pair()`,
aber die eigentliche Draw-Erzeugung läuft zentral über `core/algorithm.py`.
`DrawContext.from_request(...)` ist der bevorzugte Einstieg für alle Aufrufe,
weil damit der normalisierte Kontext für Audit und Replay einheitlich erzeugt wird.

Die Live-DB enthält zusätzlich `uq_effective_draw_per_date`: pro Tag ist genau ein
effektiver Draw erlaubt (`is_effective = TRUE`).

**SINGLE:** `pos1` = anwesendes Kind. `pos2` = NULL. `pos3` = NULL.

**SKIP:** Alle Positionsfelder NULL. Kein Fensterverbrauch.

**Zeitzone:** Der fachliche Tagesbezug verwendet die konfigurierte Controller-Zeitzone
(`controller_timezone`), nicht die Server-Standardzeit.

**Aktive Fenster:** Ein leerer Active-State ist fachlich zulässig. Wenn `active_window_present = false`
und `exactly_one_active_window = false`, bedeutet das in der aktuellen Architektur
nicht automatisch einen Fehler. Das nächste `TRIPLET`-Fenster wird beim ersten
passenden Draw automatisch erzeugt.

---

## SQL-Ausführungsreihenfolge

```
00_reset_and_create_database.sql   – als postgres
01_schema_types_and_tables.sql     – als kids_controller_admin, DB kids_controller
02_seed_system_config.sql          – als kids_controller_admin, DB kids_controller
03_add_replay_context_hash.sql     – Migration für bestehende Instanzen
verification_queries.sql           – optional, zur Prüfung
```

`03_add_replay_context_hash.sql` backfilled historische `draws`-Zeilen mit dem
bereits vorhandenen `seed_material_hash`, damit ältere Datenbestände auf das
neue Pflichtfeld umgestellt werden können, ohne sofort neu erzeugt werden zu müssen.

### Live-Migrationspfad

1. `03_add_replay_context_hash.sql` auf der Live-DB ausführen.
2. Anwendung neu deployen, damit der Code `replay_context_hash` schreibt.
3. `verification_queries.sql` ausführen und prüfen, dass die neue Spalte vorhanden ist.
4. Optional einen `TRIPLET`-Draw auslösen, wenn du direkt wieder ein aktives
   Fenster anlegen willst.

Wenn du einen harten Betriebszustand "genau ein aktives Fenster zu jedem Zeitpunkt"
erzwingen willst, braucht das einen zusätzlichen Bootstrap-Job oder eine
Bootstrapping-Regel beim Start. Die aktuelle Version ist bewusst toleranter und
kommt mit `0` aktiven Fenstern zurecht.
